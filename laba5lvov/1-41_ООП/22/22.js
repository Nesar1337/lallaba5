let arrHelper = new ArrHelper;
class Validator {
    
	isEmail(arr) {
		// сумма элементов
	}

    isDomain(arr) {
		// сумма элементов
	}

    isNumber(arr) {
		// сумма элементов
	}

}